import React from 'react'
import ReactDOM from 'react-dom'
import './index.tsx'
import App from '.'

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
)
